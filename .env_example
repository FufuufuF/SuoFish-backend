# =======================================================
# 应用基础配置
# 对应: src/core/config/settings.py
# =======================================================
APP_NAME=ChatBot
DEBUG=True

# =======================================================
# 数据库配置 (MySQL)
# 对应: src/core/config/database.py -> DatabaseSettings
# 前缀: DB_
# =======================================================
DB_HOST=localhost
DB_PORT=3306
DB_NAME=chatbot_db
DB_USER=
DB_PASSWORD=

# =======================================================
# 向量数据库配置 (Chroma)
# 对应: src/core/config/database.py -> ChromaSettings
# 前缀: CHROMA_
# =======================================================
CHROMA_PATH=./chroma_data
CHROMA_COLLECTION_NAME=documents

# =======================================================
# AI 模型配置 (LLM & Embedding)
# 对应: src/core/config/ai.py
# =======================================================
# 阿里云 DashScope API Key
DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 通义千问 LLM 配置
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
QWEN_MODEL_NAME=qwen-plus

# Embedding 模型配置
TONGYI_MODEL_NAME=text-embedding-v1

# =======================================================
# 认证配置 (JWT)
# 对应: src/core/config/auth.py
# 前缀: JWT_
# =======================================================
# 生产环境请务必修改此密钥！可以使用 openssl rand -hex 32 生成
JWT_SECRET_KEY=casku_AOasoicoij237ys98s
JWT_ALGORITHM=HS256
JWT_EXPIRATION_TIME=3600

# =======================================================
# CORS 跨域配置
# 对应: src/core/config/cors.py
# 前缀: CORS_
# =======================================================
# 多个域名用逗号分隔
CORS_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:8080
CORS_ALLOW_CREDENTIALS=True
CORS_ALLOW_METHODS=*
CORS_ALLOW_HEADERS=*

# =======================================================
# Docker 部署专用配置 (MySQL 容器初始化)
# !! 必须添加，否则数据库容器无法创建初始用户和库 !!
# =======================================================
MYSQL_ROOT_PASSWORD=
MYSQL_DATABASE=chatbot_db
MYSQL_USER=
MYSQL_PASSWORD=